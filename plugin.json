{
    "id": "com.mattermost.read-receipts",
    "name": "Read Receipts",
    "description": "WhatsApp-style read receipts for DMs, channels and threads.",
    "version": "0.1.0",
    "min_server_version": "9.5.0",
    "server": {
        "executables": {
            "linux-amd64": "server/dist/plugin-linux-amd64",
            "linux-arm64": "server/dist/plugin-linux-arm64",
            "darwin-amd64": "server/dist/plugin-darwin-amd64",
            "darwin-arm64": "server/dist/plugin-darwin-arm64",
            "windows-amd64": "server/dist/plugin-windows-amd64.exe"
        }
    },
    "webapp": {
        "bundle_path": "webapp/dist/main.js"
    },
    "settings_schema": {
        "header": "WhatsApp-style read receipts. The sender sees gray double-check when delivered and blue double-check once at least one recipient has actually viewed the message.",
        "footer": "",
        "settings": []
    }
}
